{
  "name": "keepitfresh-firestore-rules-tests",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node firestore.rules.test.mjs",
    "test:emulator": "firebase emulators:exec --project demo-keepitfresh --only firestore \"node firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^12.7.0"
  }
}
